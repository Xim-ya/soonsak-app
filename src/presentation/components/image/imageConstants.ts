/**
 * 이미지 관련 상수 정의
 *
 * 포스터/배경 이미지의 기본 비율, 에러 플레이스홀더 설정 등을 중앙 관리합니다.
 */

// ============================================================================
// 이미지 비율 상수
// ============================================================================

export const IMAGE_RATIO = {
  /** 포스터 비율 (세로형) - TMDB 표준 */
  poster: 2 / 3,
  /** 배경 비율 (가로형) - 유튜브 썸네일/TMDB backdrop 표준 */
  backdrop: 16 / 9,
} as const;

// ============================================================================
// 기본 설정
// ============================================================================

export const IMAGE_DEFAULTS = {
  /** 기본 테두리 둥글기 */
  borderRadius: 4,
  /** 기본 트랜지션 시간 (ms) */
  transitionDuration: 300,
  /** 기본 캐시 사용 여부 */
  enableCache: false,
} as const;

// ============================================================================
// 에러 플레이스홀더 설정
// ============================================================================

export const ERROR_PLACEHOLDER = {
  /** 아이콘 크기 비율 (컨테이너 대비) */
  iconSizeRatio: 0.25,
  /** 최소 아이콘 크기 */
  minIconSize: 24,
  /** 최대 아이콘 크기 */
  maxIconSize: 48,
} as const;
